{
  "name": "integration-test",
  "private": true,
  "scripts": {
    "format": "dev format",
    "lint": "dev lint",
    "test": "jest"
  },
  "devDependencies": {
    "@actions-kit/dev": "^0.2.0"
  }
}
