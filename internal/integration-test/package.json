{
  "name": "integration-test",
  "private": true,
  "scripts": {
    "format": "dev format",
    "lint": "dev lint",
    "test": "dev test"
  },
  "devDependencies": {
    "@actions-kit/dev": "^0.2.0",
    "@actions-kit/pip": "^0.1.0"
  }
}
